import { FC } from "react";

import PageTitle from "@/shared/pageTitle";

const TitleCompare: FC = () => {
    return (
        <div className={"mt-[25px]"}>
            <PageTitle title={"Сравнение товаров"} />
        </div>
    );
};

export default TitleCompare;
