import { FC } from "react";

import PageTitle from "@/shared/pageTitle";

const TitleContacts: FC = () => {
    return (
        <div className={"mt-[10px]  lgMax:mb-[15px]"}>
            <PageTitle title={"Контакты"} />
        </div>
    );
};

export default TitleContacts;
